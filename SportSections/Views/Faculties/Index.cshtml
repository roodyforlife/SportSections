@model IEnumerable<SportSections.Models.Faculty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.University)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShortName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.University.Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FacultyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FacultyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FacultyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
